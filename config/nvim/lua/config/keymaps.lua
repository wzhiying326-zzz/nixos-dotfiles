-- ============================================================================
--  全局快捷键
-- ============================================================================
--  leader = 空格（必须先设，下面所有 <leader> 才会生效）
-- ============================================================================

vim.g.mapleader      = ' '
vim.g.maplocalleader = ','

local map = vim.keymap.set

-- ---------- 清空搜索高亮 ----------
map('n', '<Esc>', '<cmd>nohlsearch<cr>', { desc = '清空搜索高亮' })

